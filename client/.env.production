# API 服务器地址（可选，默认自动推断）
VITE_API_BASE=https://jz.faith.wang/api

# CDN 静态资源地址（可选，默认为空使用本地资源）
# 配置后，构建产物中的静态资源路径将以此为前缀
# 例如：VITE_CDN_BASE=https://cdn.example.com/jiuzhou/
VITE_CDN_BASE=https://cdn.faith.wang/jiuzhou/
